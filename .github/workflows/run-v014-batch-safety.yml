name: Run v0.1.4 batch eligibility hardening

on:
  push:
    branches:
      - agent/v0.1.4-navigation-zenpm
    paths:
      - scripts/apply_v014_batch_safety.py
      - .github/workflows/run-v014-batch-safety.yml

jobs:
  patch:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/v0.1.4-navigation-zenpm
          fetch-depth: 0
      - name: Apply batch eligibility hardening
        run: python3 scripts/apply_v014_batch_safety.py
      - name: Refresh updater hashes
        run: python3 scripts/generate_update_manifest.py
      - name: Validate patch
        run: |
          python3 -m py_compile scripts/*.py
          git diff --check
          sudo apt-get update
          sudo apt-get install -y lua5.1
          find . -name '*.lua' -print0 | xargs -0 -n1 luac5.1 -p
          lua5.1 tests/v014_expansion.lua
      - name: Commit batch safety fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/apply_v014_batch_safety.py
          git add lib/matcher.lua main.lua providers/googlebooks.lua providers/openlibrary.lua providers/hardcover.lua providers/amazon.lua tests/v014_expansion.lua docs/ROADMAP_IMPLEMENTATION_CHECKLIST.md CHANGELOG.md update.json
          git commit -m "Block conflicted matches from v0.1.4 batch auto-apply"
          git push origin HEAD:agent/v0.1.4-navigation-zenpm
